#!/usr/bin/env node

SpriteGen = require('../lib/spritegen');

try {
    var spriteGen = new SpriteGen({ dir: 'D:/git/narrative-prototype/public/images' });
    spriteGen.execute();
}
catch (err) {
    console.log(err.message);

    // Remove this later.
    throw err;
}